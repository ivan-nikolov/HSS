<div>
    <h6>Selected category: @categoryName</h6>
</div>
<input name="ParentCategoryId" class="form-control" value="@this.selectedCategoryId" type="hidden" />
<CategoriesDropdownComponent UpdateRoot="Update" CategoryToEdit="CategoryToEdit" CurrentCategoryId="null"></CategoriesDropdownComponent>

@code {
    private int? selectedCategoryId;

    [Parameter]
    public int? CategoryToEdit { get; set; }

    private string categoryName;

    private void Update((int? id, string name) category)
    {
        this.selectedCategoryId = category.id;
        this.categoryName = category.name;
    }
}
